'use strict';

const Boom = require('boom');

/**
 * Operations on /data
 */
module.exports = {
    /**
     * summary: Create a data
     * description: 
     * parameters: data
     * produces: 
     * responses: 201, default
     */
    post: function createdata(request, h) {
    //    return Boom.notImplemented();
        return null;
    }
};
